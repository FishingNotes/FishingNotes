<?xml version="1.0" encoding="utf-8"?>

<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:elevation="8dp"
    android:layout_margin="4dp">

<androidx.constraintlayout.widget.ConstraintLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <ImageView
        android:id="@+id/button_add_new_catch"
        android:layout_width="48dp"
        android:layout_height="48dp"
        app:tint="@color/primaryFigmaColor"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_baseline_add_location_24" />

    <TextView
        android:id="@+id/tv_add_new_catch"
        style="@style/TextAppearance.AppCompat.SecondaryText"
        android:text="@string/add_new_place"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/button_add_new_catch"
        app:layout_constraintStart_toStartOf="@+id/button_add_new_catch"
        app:layout_constraintTop_toBottomOf="@+id/button_add_new_catch" />
</androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>